<!DOCTYPE html>
<html>
  <head>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag 'header.css' %>
    <%= stylesheet_link_tag 'welcome-style.css' %>
  </head>
  <body>
    <div class="main-header">
        <%= image_tag "pics/logo.png", :class => "logo"%>
        <%= button_to "Logout", sessions_path, method: :delete, :class => "button" %>
    </div>
    <div class="page">
      <h1 class="title">Add A Specific Room</h1>
        <%= form_for [@user, @property, @room] do |form| %>
          <%= form.label :room_type %>
          <%= form.text_field :room_type %>
          <%= form.label :price %>
          <%= form.text_field :price %>
          <%= form.label :smoking %>
          <%= form.check_box :smoking %>
          <%= form.label :room_number %>
          <%= form.text_field :room_number %>

          <%= form.submit "Submit"%>
        <% end %>

        <p><%= link_to "Back", user_property_rooms_path(@user, @property) %>
    </div>
  </body>
</html>