<!DOCTYPE html>
<html>
  <head>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag 'header.css' %>
    <%= stylesheet_link_tag 'welcome-style.css' %>
  </head>
  <body>
    <div class="main-header">
        <%= image_tag "pics/logo.png", :class => "logo"%>
        <%= button_to "Logout", sessions_path, method: :delete, :class => "button" %>
    </div>
    <div class="page">
      <h1 class="title">All Rooms</h1>
      <% @rooms.each do |room| %>
        <p><%= link_to room.room_type, user_property_room_path(@user, @property, room) %></p>
      <% end %>
      <p><%= link_to "Add Rooms", new_user_property_room_path(@user, @property) %></p>
      <p><%= link_to "Back", user_property_path(@user, @property) %>
    </div>
  </body>
</html>



