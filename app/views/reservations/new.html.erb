<!DOCTYPE html>
<html>
  <head>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag 'header.css' %>
    <%= stylesheet_link_tag 'welcome-style.css' %>
  </head>
  <body>
    <div class="main-header">
        <%= image_tag "pics/logo.png", :class => "logo"%>
        <%= button_to "Logout", sessions_path, method: :delete, :class => "button" %>
    </div>
    <div class="page">
      <h1 class="title">Please Enter Check In and Check Out dates</h1>
        <%= form_for [@user, @property, @guest, @reservation] do |f| %>
        <%= f.label :date_in %>
        <%= f.date_field :date_in, class: 'form-control' %>  
        <%= f.label :date_out %>
        <%= f.date_field :date_out, class: 'form-control' %>
        <%= f.submit "Submit" %>
      <% end %>

      <p><%= link_to "Back", user_property_guest_path(@user, @property, @guest) %><p>
    </div>
  </body>
</html>

<h1></h1>

